## PurePromise Backend - Environment Variables Example
## Copy this file to `.env` and fill in real values.

# Server
# Optional: port for the backend to listen on (defaults to 8080)
PORT=8080

# AI Service (Fal)
FAL_KEY=your_fal_ai_api_key

# S3 / Object Storage
S3_ACCESS_KEY=your_s3_access_key
S3_SECRET_KEY=your_s3_secret_key
BUCKET_NAME=your_bucket_name
# For example: https://s3.ap-south-1.amazonaws.com
ENDPOINT=your_s3_endpoint_url

# Authentication / JWT
# Symmetric key used to sign your own JWTs for internal auth
AUTH_JWT_KEY=your_internal_jwt_signing_key

# Clerk Authentication
# Public key used to verify Clerk-issued JWTs
CLERK_JWT_PUBLIC_KEY=your_clerk_jwt_public_key
# Clerk issuer URL, e.g. https://your-app.clerk.accounts.dev
CLERK_ISSUER=your_clerk_issuer_url
# Clerk secret key for server-side API calls (if needed by backend)
CLERK_SECRET_KEY=your_clerk_secret_key

# Clerk Webhook Signing Secret
# Used to verify signatures on Clerk webhooks in /api/webhook/clerk
SIGNING_SECRET=your_clerk_webhook_signing_secret

# URLs
# Public base URL where this backend is reachable from Fal/webhooks, e.g. ngrok URL in dev
WEBHOOK_BASE_URL=https://your-public-backend-url
# Frontend URL used for redirects / CORS
FRONTEND_URL=http://localhost:3000

# Node environment (optional)
#NODE_ENV=development

